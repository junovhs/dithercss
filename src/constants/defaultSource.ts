// The default demo animation shown in the editor on first load: a self-contained
// "sleeping orb". Kept as a real .html file and imported raw so it stays editable
// as markup rather than an escaped JS string.
import defaultSource from "./defaultSource.html?raw";

export const DEFAULT_SOURCE: string = defaultSource;
